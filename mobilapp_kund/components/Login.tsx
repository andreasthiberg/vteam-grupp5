import { View, Text, Image } from 'react-native';
import { Base, Typography } from '../styles';

export default function Home() {
    return (
        <View style={Base.home}>
            <Text style={Typography.header2}>Login</Text>
        
        </View>
    );
}